# Reference

Use these pages when integrating a script, agent, or future UI with Optimist:

- [CLI reference](./cli.md)
- [HTTP and WebSocket API](./http-api.md)
- [YAML project format](./yaml.md)

The source of truth remains the typed Rust API and `optimist <command> --help`. The repository enables `#![deny(missing_docs)]`, so public Rust items must remain documented and warning-free.
